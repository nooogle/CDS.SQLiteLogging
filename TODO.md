# TODO

Small, low-priority maintenance items that don't warrant their own design doc.

- [ ] `actions/attest-sbom` is deprecated in favor of `actions/attest` — update
      `.github/workflows/release.yml`'s "Attest SBOM" step. Flagged as a deprecation warning
      (not a failure) in the `V2.6.4` release run (2026-08-14).
